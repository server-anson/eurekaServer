spring:
  application:
    name: eureka-service
#  cloud:
#    config:
#      uri: http://192.168.18.55:7003
#      label: xjg
#      profile: xx,ss
server:
  port: 2222
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    fetchRegistry: false
